
    <?php
    /*
    $id = 0;
    if(isset($_POST['dni'])){
        $con =  new mysqli('localhost','joaquim','joaquim','users');
    //REVISAR PDF https://hackmd.io/@JdaXaviQ/BkqzQq5vt PARA EL TEMA DE MYSQL -> ENTRAR SUDO MYSQL
        $consulta= $con->prepare("INSERT INTO client (id_user,user,pwd) VALUES (?,?,?);");
        $id++;
        $user = $_POST['user'];
        $cont = $_POST['password'];
        $cont2 = password_hash($cont, PASSWORD_BCRYPT);
        
        $consulta->bind_param('iss', $id, $user, $cont2);
        echo "Registrado exitosamente!";
        
        $consulta->execute();
        //header("Location: http://localhost/M7/UF3/practica_botiga/botiga.php");

        $consulta->close();
    }
    class login{
        
    }
*/
    ?>
